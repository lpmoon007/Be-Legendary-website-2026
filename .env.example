# Copy to ".env" and fill in. Astro exposes PUBLIC_* vars to the browser.

# GA4 Measurement ID (e.g. G-XXXXXXXXXX). Analytics + conversion events load
# only when this is set; leave empty to disable. Read at BUILD time, so rebuild
# after changing it.
PUBLIC_GA_ID=
